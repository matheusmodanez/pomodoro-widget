--> Add a change background functionality.
    - On the Settings components, create the function to select the color or image of the background, and then, pass it as a parameter on the Component call in the App.js file.

--> Tasks don`t persist when page is loaded (I`ll have to use a storage... maybe I can use the Notion API to do that)